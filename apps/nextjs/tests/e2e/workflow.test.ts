import { shortest } from "@antiwork/shortest";

shortest([
  "Sign in as support@gumroad.com",
  "Go to the Settings page and create a workflow",
  "Check that the workflow is saved by checking that it is visible in the list of workflows",
]);
